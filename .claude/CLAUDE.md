@../README.md

## Instructions

Always update README.md when any information in it becomes out of date (file paths, script names, workflow steps, etc.).

After editing any `.qmd` file, run `Rscript R/make_package_map.R` to regenerate `data/package_map.json`. Do this without asking — it's a local, reversible action that's a no-op when nothing relevant changed.
